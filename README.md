To run the project
1. run ```git clone https://github.com/Mee1296/merge_primo.git```
2. run ```npm install --save-dev typescript ts-node jest ts-jest @types/jest```
3. run demo: ```npm run run```
4. run test: ```npm test```
